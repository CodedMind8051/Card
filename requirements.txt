# Student ID Card Automation - Python dependencies
# Install with:  python -m pip install -r requirements.txt

# Image processing / rendering
numpy>=1.24.0
# Pinned: 5.x is a broken/empty build (no CascadeClassifier / cv2.data)
opencv-python==4.10.0.84
Pillow>=10.0.0

# Web UI (template designer + photo/field editor)
flask>=3.0.0
werkzeug>=3.0.0

# Gemini API (free tier) - replaces Playwright Google Lens scraping
google-genai>=0.3.0
# Groq fallback (free tier) - 30 RPM / 1000 RPD vision model for when Gemini 503/429s
groq>=0.11.0

# Browser automation (kept for template designer/editor only - not needed for scraping)
playwright>=1.40.0